import React, { Component } from 'react'

export class Count extends Component {

    constructor(props) {
        super(props)
        this.state = {
             
        }
    }

    render() {
        return (
            <div>
                <a onClick={} className='decrement'>-</a>
                <a onClick={} className='increment'>+</a>

            </div>
        )
    }
}

export default Count
